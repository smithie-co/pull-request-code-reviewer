# GitHub Configuration for Local Development
GITHUB_TOKEN="your_personal_access_token_with_repo_scope"
# Path to a local JSON file containing a GitHub pull_request event payload
# See: https://docs.github.com/en/webhooks-and-events/webhooks/webhook-events-and-payloads#pull_request
GITHUB_EVENT_PATH="path/to/your/sample_event.json"
# The repository slug (e.g., your_username/your_test_repo) for local testing
GITHUB_REPOSITORY="your_username/your_test_repo"
# Optional: An alternative GitHub token if GITHUB_TOKEN is not sufficient for specific tasks
# CALLING_REPO_TOKEN=""

# AWS Configuration
AWS_ACCESS_KEY_ID="your_aws_access_key_id"
AWS_SECRET_ACCESS_KEY="your_aws_secret_access_key"
# The AWS region where your Bedrock models are available (e.g., us-east-1)
AWS_DEFAULT_REGION="us-east-1"

# AWS Bedrock Model IDs
HEAVY_MODEL_ID="anthropic.claude-v2" # Or your preferred model for detailed analysis
LIGHT_MODEL_ID="anthropic.claude-instant-v1" # Or your preferred model for summarization
DEEPSEEK_MODEL_ID="meta.llama2-13b-chat-v1" # Or your preferred model for refinement

# Optional: Define a custom bot name to be used in comments
# BOT_NAME="my-dev-bot[bot]" 